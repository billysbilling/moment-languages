module.exports = function(grunt) {
    grunt.initConfig({
        'billy-builder': {
            title: 'moment-languages',
            jshint: true
        }
    });

    grunt.loadNpmTasks('billy-builder');
};